# Lambda-specific requirements (optimized for size)

# LangGraph & LangChain
langgraph>=0.2.0
langchain>=0.3.0
langchain-aws>=0.2.0
# Note: langchain-community removed to save space (not used directly)

# API Framework
fastapi>=0.115.0
pydantic>=2.9.0
pydantic-settings>=2.6.0
# Note: uvicorn not needed for Lambda (Mangum handles it)

# AWS
boto3>=1.35.0

# Data Processing
pandas>=2.2.0
numpy>=1.26.0

# Vector Store - REMOVED (not used)
# faiss-cpu>=1.8.0

# Utilities
python-dotenv>=1.0.0
httpx>=0.27.0
tenacity>=9.0.0

# AWS Lambda adapter
mangum>=0.17.0

# SQL for KPI queries
duckdb>=0.10.0

# Note: scikit-learn REMOVED (using numpy for cosine similarity)
